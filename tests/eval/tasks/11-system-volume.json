{
  "id": "system-set-volume",
  "category": "system",
  "prompt": "Set the system volume to 42 percent",
  "why": "A setting with a number that can be read straight back out of Windows. No ambiguity about whether it worked. The teardown puts the volume back where it was, which is why the setup records it first. Manual: it changes something the user can hear.",
  "timeoutMs": 90000,
  "setup": [
    { "run": "& '{fixtures}/read-volume.ps1' | Set-Content -Path '{workspace}/volume-before.txt'" },
    { "run": "& '{fixtures}/set-volume.ps1' -Percent 11" }
  ],
  "verify": [
    { "run": "& '{fixtures}/read-volume.ps1'", "expect": "42" }
  ],
  "teardown": [
    { "run": "$v = Get-Content -Path '{workspace}/volume-before.txt' -ErrorAction SilentlyContinue; if ($v) { & '{fixtures}/set-volume.ps1' -Percent ([int]$v) }" }
  ],
  "manual": true
}
